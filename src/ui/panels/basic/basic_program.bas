100 REM JOYPORT TEST PROGRAM
200 POKE - 16296,0: REM ANNUNCIATOR 0 OFF
210 POKE - 16294,0: REM ANNUNCIATOR 1 OFF
300 POKE - 16368,0: REM CLEAR KEYBOARD STROBE
400 TEXT : HOME : REM CLEAR SCREEN
500 HOME : PRINT "JOYPORT TEST PROGRAM": PRINT : PRINT
600 PRINT "PLUG IN JOYPORT TO TEST AND PLUG TWO": PRINT "ATARI JOYSTICKS AND TWO SETS OF GAME PADDLES TO IT."
700 PRINT "SET THE FRONT SWITCH TO THE FRONT (ATARI) AND THE BACK SWITCH TO THE RIGHT"
800 REM JT$ = "RIGHT": POKE - 16296,0: POKE - 16294,0: GOSUB 1300: REM TEST RIGHT ATARI STICK
900 REM POKE - 16296,0: POKE - 15294,0: JT$ = "LEFT": GOSUB 3500: REM TEST LEFT ATARI STICK
1000 GOSUB 3900: REM READ BOTH ATARI STICKS WITH BACK SWITCH CENTERED
1100 GOSUB 4400: END : REM PADDLE READ
1200 HOME : PRINT "OK, THAT JOYPORT WAS OK. PLUG ANOTHER ONE IN AND PRESS SPACE TO RERUN THE TEST.": GET A$: RUN
1300 POKE - 16368,0: REM CLEAR KEYBOARD STROBE
1400 PRINT "PRESS SPACE WHEN READY TO START": GET A$: POKE - 16368,0
1500 HOME : PRINT "TILT THE "JT$" ATARI JOYSTICK RIGHT"
1600 FOR X = 1 TO 1000: IF PEEK(-16285) < 128 THEN 1900: REM CHECK BUTTON 0
1700 NEXT X: REM WAIT LOOP. IF NO RESPONSE THE TESTER DIED THE JOYPORT FAILED, THE SWITCHES WERE SET WRONG OR HELL FROZE OVER. MAKE THEM START OVER AGAIN.
1800 GOTO 3800
1900 HOME : PRINT "OK THAT WORKS, NOW TILT THE JOYSTICK LEFT"
2000 FOR X = 1 TO 1000: IF PEEK(-16286) < 128 THEN 2300: REM BUTTON1
2100 NEXT X: REM SEE NOTES ON 1700
2200 GOTO 3800
2300 HOME : PRINT "OK, THAT WORKED, NOW PUSH THE FIRE BUTTON"
2400 FOR X = 1 TO 1000: IF PEEK(-16287) < 128 THEN 2700: REM BUTTON 2
2500 NEXT X
2600 GOTO 3800
2700 HOME : POKE - 16293,0: REM READ VERTICAL BUTTONS
2800 HOME : PRINT "NOW PRESS THE JOYSTICK UP": FOR X = 1 TO 1000
2900 IF PEEK(-16286) < 128 THEN 3200
3000 NEXT
3100 GOTO 3800
3200 HOME : PRINT "NOW TILT THE JOYSTICK DOWN": FOR X = 1 TO 1000: IF PEEK(-16285) < 128 THEN POKE - 16294,0: RETURN : REM END OF LOOP. TURN AN0 BACK OFF
3300 NEXT
3400 GOTO 3800: REM LOOP ENDED WITH NO BUTTONS DETECTED
3500 HOME : PRINT "NEXT PRESS THE BACK SWITCH TO THE LEFT' AND WE'LL REPEAT THE TEST ON THE OTHER JOYSTICK"
3600 GOSUB 1300: RETURN
3700 END
3800 HOME : PRINT CHR$(7); "NOTHING HAPPENED. CHECK THE SWITCH SETTINGS AND CONNECTIONS THEN PRESS SPACE TO START OVER": GET A$: RUN
3900 REM ROUTINE TO READ BOTH ATARI JOYSTICKS BY TOGGLING ANNUNCIATOR ZERO
4000 PRINT "NOW WE'LL TEST THE MIDDLE POSITION OF THE REAR SWITCH": PRINT "PRESS THE SPACE BAR WHEN YOU HAVE IT SET"
4100 POKE - 16296,0: JT$ = "LEFT": GOSUB 1300: REM TEST LEFT STICK
4200 HOME : PRINT "NOW TRY THE RIGHT STICK"
4300 POKE - 16295,0: JT$ = "RIGHT": GOSUB 1300: REM TEST RIGHT STICK
4400 HOME : PRINT "NOW WE'LL TEST THE PADDLE READINGS. PUT THE REAR SWITCH TO THE RIGHT AND THE FRONT SWITCH TO THE REAR (APPLE)"
4500 PRINT "PRESS THE SPACE BAR WHEN READY"; : GET A$
4600 Y = 0: BT = (-16287): GOSUB 5300: PRINT CHR$(7); : Y = 1: BT = (-16286): GOSUB 5300: PRINT CHR$(7); : REM Y IS THE PADDLE TO READ AND BT IS THE BUTTON TO READ
4700 HOME : PRINT "NOW MOVE THE REAR SWITCH LEFT TO TEST THE OTHER PADDLES. PRESS SPACE WHEN READY TO CONTINUE"; : GET A$: Y = 0: BT = (-16287): GOSUB 5300: PRINT CHR$(7); : Y = 1: BT = (-16286): GOSUB 5300: PRINT CHR$(7)
4800 HOME : PRINT "NEXT WE TEST THE MIDDLE POSITION OF THE REAR SWITCH. PRESS THE SPACE BAR WHEN READY TO PROCEED"; : GET A$
4900 Y = 0: BT = (-16287): GOSUB 5300: REM READ PADDLE 0 AND BUTTON0
5000 Y = 1: BT = (-16286): GOSUB 5300: REM READ PADLE 1 AND BUTTON1
5100 Y = 2: BT = (-16287): POKE - 16295,0: GOSUB 5300: REM READ PADDLE2 AND BUTTON0
5200 Y = 3: BT = (-16286): GOSUB 5300: REM READ PADDLE3 AND BUTTON1
5250 RETURN
5300 REM PADDLE AND BUTTON TEST FOLLOW
5400 HOME
5500 PRINT "TWIST THE KNOB ON PADDLE "Y" LEFT AND RIGHT QUICKLY"
5600 FOR X = 1 TO 1000
5700 IF PDL(Y) < 100 THEN 6000
5800 NEXT X
5900 GOTO 3800
6000 IF PDL(Y) > 200 THEN 6300
6100 NEXT X
6200 GOTO 3800
6300 GOSUB 6400: RETURN : REM GO TEST BUTTON
6400 PRINT CHR$(7); : HOME : PRINT "OK, NOW PUSH THE BUTTON": FOR X = 1 TO 1000: IF PEEK(BT) > 127 THEN RETURN
6500 NEXT X
6600 GOTO 3800: REM NOTHING DETECTED GO TO THE ERROR ROUTINE
